<hc:Window x:Class="RTL.Views.MainView"
           xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
           xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
           xmlns:hc="https://handyorg.github.io/handycontrol"
           xmlns:local="clr-namespace:RTL.Views"
           xmlns:s="https://github.com/canton7/Stylet"
           Title="MainView"
           Width="1100"
           Height="720"
           MinWidth="1100"
           MinHeight="720"
           WindowState="Maximized"
           WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <!-- Для SideMenu -->
            <ColumnDefinition Width="*"/>
            <!-- Для основного контента -->
        </Grid.ColumnDefinitions>

        <!-- SideMenu -->
        <hc:SideMenu Grid.Column="0" Width="200">
            <hc:SideMenuItem Header="RTL-SW" Command="{s:Action NavigateToRtlSw}"/>
            <hc:SideMenuItem Header="Настройки" Command="{s:Action NavigateToSettings}"/>
        </hc:SideMenu>

        <!-- Основной контент -->
        <ContentControl Grid.Column="1" s:View.Model="{Binding ActiveItem}"/>
    </Grid>
</hc:Window>