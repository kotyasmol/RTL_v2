<UserControl x:Class="RTL.Views.RtlPoeView"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:local="clr-namespace:RTL.Converters"
             HorizontalAlignment="Stretch"
             VerticalAlignment="Stretch"
             MinWidth="1024"
             MinHeight="768">
    <UserControl.Resources>
        <local:BoolToColorConverter x:Key="BoolToColorConverter"/>
        <local:BoolToTextConverter x:Key="BoolToTextConverter"/>
        <local:UShortToColorConverter x:Key="UShortToColorConverter"/>
        <Style x:Key="RoundButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="50"/>
            <Setter Property="Height" Value="50"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Background" Value="#B5E61D"/>
        </Style>
    </UserControl.Resources>
    <Viewbox Stretch="Uniform">
        <Grid Width="1920" Height="1080">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <!-- Верхняя часть: изображение + Expander -->
            <DockPanel Grid.Row="0" LastChildFill="False" Margin="0">
                <!-- Изображение -->
                <Viewbox Stretch="Uniform" Margin="0,0,0,0">
                    <Grid>
                        <Image Source="/TFortisBoard;component/Resources/Images/PlateImages/RTL_POE.png" Stretch="Uniform" Margin="0,0,656,0" />
                        <Canvas>
                            <Rectangle Canvas.Left="20" Canvas.Top="7" Width="300" Height="80" 
                                   Fill="Red" Opacity="0.8" Stroke="Black" StrokeThickness="2"/>
                        </Canvas>
                    </Grid>
                </Viewbox>
                <!-- Expander -->
                <Expander Header="Дополнительно" IsExpanded="True" Width="300" VerticalAlignment="Top">
                    <StackPanel Margin="10,10,10,10">
                        <TextBlock Text="Здесь можно что-то отобразить" FontSize="14"/>
                        <!-- сюда можно вставить другие элементы -->
                    </StackPanel>
                </Expander>
            </DockPanel>
            <!-- Нижняя часть: логи и прогресс -->
            <Grid Grid.Row="1" Margin="10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="108*"/>
                    <ColumnDefinition Width="367*"/>
                    <ColumnDefinition Width="475*"/>
                </Grid.ColumnDefinitions>
                <!-- Левая колонка: логи -->
                <StackPanel Grid.Column="0" Grid.ColumnSpan="2">
                    <TextBlock Text="Логи" FontSize="16" FontWeight="Bold" Margin="0,0,0,5"/>
                    <ListBox x:Name="LogListBox"
                             ItemsSource="{Binding Logs}"
                             VerticalAlignment="Stretch"
                             HorizontalAlignment="Stretch"
                             ScrollViewer.HorizontalScrollBarVisibility="Auto"
                             ScrollViewer.VerticalScrollBarVisibility="Auto">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <TextBox Text="{Binding Message}" 
                                         Foreground="{Binding Color}" 
                                         FontSize="14" FontWeight="Bold"
                                         IsReadOnly="True"
                                         BorderThickness="0"
                                         Background="Transparent"
                                         TextWrapping="NoWrap"/>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </StackPanel>
                <!-- Правая колонка: прогресс и элементы -->
                <StackPanel Grid.Column="2" Margin="20,0,0,0">
                    <TextBlock Text="Прогресс" FontSize="16" FontWeight="Bold" Margin="0,0,0,5"/>
                    <ProgressBar Minimum="0" Maximum="100"
                                 Height="25"
                                 Value="{Binding ProgressValue}"/>
                    <!-- Здесь можно добавить другие элементы (кнопки, статусы и пр.) -->
                </StackPanel>
            </Grid>
        </Grid>
    </Viewbox>
</UserControl>
